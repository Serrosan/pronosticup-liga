services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - "80"
    volumes:
      - 'storage-app:/var/www/html/storage/app/public'
    environment:
      APP_ENV: production
      APP_DEBUG: 'false'
volumes:
  storage-app:
    driver: local